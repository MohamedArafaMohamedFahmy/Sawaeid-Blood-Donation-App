<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


  <item
      android:id="@+id/action_search"
      android:icon="@drawable/ic_baseline_search_24"
      android:title="@string/search_by_phone_number_or_name"
      app:actionViewClass="androidx.appcompat.widget.SearchView"
      app:showAsAction="ifRoom|collapseActionView" />

  <item android:id="@+id/action_add_donor"
      android:icon="@drawable/ic_baseline_add_donor_24"
      android:title="@string/add_donor"
      app:showAsAction="ifRoom"/>

  <item android:id="@+id/action_sign_out"
      android:icon="@drawable/ic_baseline_loginout"
      android:title="@string/sign_out"
      app:showAsAction="ifRoom"/>
</menu>